# Copy to `.env` and fill in values.

# SQLite file in the project root (same default as `src/lib/prisma.ts`)
DATABASE_URL=file:./dev.db

# Google OAuth (optional for local UI; required for "Sign in with Google")
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
